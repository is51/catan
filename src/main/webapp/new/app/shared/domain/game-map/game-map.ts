export class GameMap {
    //TODO: fill other properties...
    constructor(params) {
        //this.gameId = params.gameId;
    }

    update(params) {
        //TODO: revise this method
        //this.gameId = params.gameId;
    }
}