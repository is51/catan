//TODO: replace it with Class
//TODO: add method "getDisplayedName" and replace 'user.username' every where

export interface User {
    firstName: string;
    guest: boolean;
    id: number;
    lastName: string;
    username: string;
}